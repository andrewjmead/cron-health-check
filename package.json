{
	"name": "cron-health-check",
	"private": true,
	"description": "Dev environment tooling for the Cron Health Check plugin.",
	"devDependencies": {
		"@wordpress/env": "^10.0.0"
	},
	"scripts": {
		"env:start": "wp-env start",
		"env:stop": "wp-env stop",
		"test:integration": "wp-env run tests-cli --env-cwd=wp-content/plugins/cron-health-check vendor/bin/phpunit -c phpunit-integration.xml.dist"
	}
}
